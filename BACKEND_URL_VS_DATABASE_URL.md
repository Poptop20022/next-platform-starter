# Backend URL vs Database URL - В чём разница?

## 🔑 Ключевое различие

**Backend URL** и **Database URL** - это **РАЗНЫЕ вещи**!

## 📍 Backend URL (URL вашего API сервера)

### Что это:
URL вашего **Node.js/Express приложения** на Railway, которое обрабатывает API запросы.

### Где используется:
- В **Netlify** как `NEXT_PUBLIC_API_URL`
- В **браузере** для запросов к API
- На **странице входа** в поле "API URL"

### Где найти:

**Railway Dashboard:**
1. Откройте ваш проект на Railway
2. Найдите **backend сервис** (Node.js/Web Service, НЕ PostgreSQL!)
3. **Settings** → **Networking**
4. Скопируйте **Public Domain**
5. Добавьте `https://` в начало

**Пример:**
```
https://tenderhub-production.up.railway.app
```

### Как проверить:
Откройте в браузере:
```
https://your-backend.railway.app/api/health
```

Должен вернуться:
```json
{"status":"ok","timestamp":"2025-..."}
```

---

## 🗄️ Database URL (Connection String для БД)

### Что это:
Connection string для подключения к **PostgreSQL базе данных** на Neon.

### Где используется:
- В **Railway** как переменная `DATABASE_URL` в backend сервисе
- Backend использует его для подключения к базе данных
- **НЕ используется** в frontend!

### Где найти:

**Neon Dashboard:**
1. Откройте https://console.neon.tech
2. Войдите в аккаунт
3. Выберите ваш проект
4. Выберите базу данных
5. **Connection Details** → **Connection string**
6. Скопируйте connection string

**Пример:**
```
postgresql://neondb_owner:npg_YkX7Wm6FNRTU@ep-mute-truth-aejonffm-pooler.c-2.us-east-2.aws.neon.tech/neondb?channel_binding=require&sslmode=require
```

### Где настроить:
**Railway Dashboard:**
1. Ваш **backend сервис** (Node.js)
2. **Variables**
3. Добавьте: `DATABASE_URL` = ваш Neon connection string

---

## 📊 Сравнительная таблица

| Параметр | Backend URL | Database URL |
|----------|-------------|--------------|
| **Что это** | URL API сервера | Connection string к БД |
| **Где находится** | Railway (backend сервис) | Neon (база данных) |
| **Где используется** | Netlify (frontend) | Railway (backend) |
| **Формат** | `https://...railway.app` | `postgresql://...` |
| **Для чего** | HTTP запросы к API | SQL запросы к БД |
| **Кто использует** | Frontend (браузер) | Backend (сервер) |

---

## 🎯 Что вам нужно сейчас

### Для страницы входа нужен: **Backend URL**

1. **Railway Dashboard** → ваш проект
2. Найдите **backend сервис** (Node.js приложение)
   - Если его нет → создайте по инструкции [SETUP_BACKEND_RAILWAY.md](./SETUP_BACKEND_RAILWAY.md)
3. **Settings** → **Networking** → **Public Domain**
4. Скопируйте и добавьте `https://` в начало

**Пример:**
```
https://tenderhub-production.up.railway.app
```

Введите этот URL в поле "API URL" на странице входа.

---

## ❓ Частые вопросы

### Q: У меня есть только Database URL, где Backend URL?
**A:** Backend URL - это отдельный сервис на Railway. Если его нет, создайте по инструкции [SETUP_BACKEND_RAILWAY.md](./SETUP_BACKEND_RAILWAY.md)

### Q: Могу ли я использовать Database URL как Backend URL?
**A:** Нет! Database URL - это для подключения к БД, а Backend URL - это ваш API сервер. Это разные вещи.

### Q: Где находится Backend URL?
**A:** На Railway, в вашем backend сервисе (Node.js приложение). Если сервиса нет - его нужно создать.

### Q: Как узнать, есть ли у меня backend сервис на Railway?
**A:** 
- Откройте Railway Dashboard
- Посмотрите список сервисов
- Если есть сервис типа "Web Service" или "Node.js" - это backend
- Если только PostgreSQL - backend нет, нужно создать

---

## 🔍 Визуализация

```
┌─────────────────────────────────┐
│   Frontend (Netlify)            │
│   Нужен: Backend URL            │
│   ────────────────────────────  │
│   NEXT_PUBLIC_API_URL =         │
│   https://backend.railway.app   │ ← Это Backend URL
└──────────────┬──────────────────┘
               │ HTTP запросы
               ▼
┌─────────────────────────────────┐
│   Backend (Railway)             │
│   Использует: Database URL      │
│   ────────────────────────────  │
│   DATABASE_URL =                │
│   postgresql://neon...          │ ← Это Database URL
└──────────────┬──────────────────┘
               │ SQL запросы
               ▼
┌─────────────────────────────────┐
│   Database (Neon)               │
│   PostgreSQL                    │
└─────────────────────────────────┘
```

---

## ✅ Чек-лист

- [ ] Понял разницу между Backend URL и Database URL
- [ ] Нашел/создал backend сервис на Railway
- [ ] Получил Backend URL (Public Domain)
- [ ] Ввел Backend URL в форму входа
- [ ] Проверил подключение (должно быть ✅)

