# Переменные окружения для TenderHub

## 🔵 Для Frontend (Next.js на Render/Netlify/Cloudflare Pages)

### Обязательные переменные:

| Key | Value | Описание |
|-----|-------|----------|
| `NEXT_PUBLIC_API_URL` | `https://ваш-backend.railway.app` | URL вашего backend API (БЕЗ слеша в конце!) |

**Пример:**
```
NEXT_PUBLIC_API_URL=https://tenderhub-production.up.railway.app
```

⚠️ **Важно:**
- URL должен начинаться с `https://`
- НЕ должно быть слеша `/` в конце
- Префикс `NEXT_PUBLIC_` обязателен (чтобы переменная была доступна в браузере)

---

## 🔴 Для Backend (Express на Render/Railway)

### Обязательные переменные:

| Key | Value | Описание |
|-----|-------|----------|
| `DATABASE_URL` | Connection string | Строка подключения к PostgreSQL |
| `PORT` | `3001` | Порт, на котором работает сервер |
| `JWT_SECRET` | Длинная случайная строка | Секретный ключ для JWT токенов |
| `NODE_ENV` | `production` | Режим работы (production/development) |

### Примеры значений:

#### Если используете Neon/Supabase (внешняя БД):

```
DATABASE_URL=postgresql://user:password@ep-xxx-xxx.region.aws.neon.tech/database?sslmode=require
PORT=3001
JWT_SECRET=ваш-очень-длинный-секретный-ключ-минимум-32-символа-случайные-буквы-и-цифры
NODE_ENV=production
```

#### Если используете Railway PostgreSQL:

```
DATABASE_URL=${{Postgres.DATABASE_URL}}
PORT=3001
JWT_SECRET=ваш-очень-длинный-секретный-ключ-минимум-32-символа-случайные-буквы-и-цифры
NODE_ENV=production
```

⚠️ **Важно:**
- Замените `Postgres` на точное имя вашего PostgreSQL сервиса на Railway
- `JWT_SECRET` должен быть длиной минимум 32 символа
- Используйте случайные символы для `JWT_SECRET` (можно сгенерировать: `openssl rand -base64 32`)

---

## 📝 Опциональные переменные для Backend:

Эти переменные имеют значения по умолчанию, но можно настроить:

| Key | Значение по умолчанию | Описание |
|-----|----------------------|----------|
| `DB_HOST` | Из `DATABASE_URL` | Хост базы данных |
| `DB_PORT` | `5432` | Порт базы данных |
| `DB_NAME` | Из `DATABASE_URL` | Имя базы данных |
| `DB_USER` | Из `DATABASE_URL` | Пользователь БД |
| `DB_PASSWORD` | Из `DATABASE_URL` | Пароль БД |
| `JWT_EXPIRES_IN` | `7d` | Время жизни JWT токена |
| `LOG_LEVEL` | `info` | Уровень логирования |
| `MAX_FILE_SIZE` | `10485760` (10MB) | Максимальный размер файла |

---

## 🎯 Быстрая инструкция для Render

### Если разворачиваете Frontend (Static Site):

1. Откройте настройки вашего сайта на Render
2. Перейдите в раздел **Environment**
3. Нажмите **Add Environment Variable**
4. Добавьте:
   - **Key:** `NEXT_PUBLIC_API_URL`
   - **Value:** `https://ваш-backend.railway.app` (замените на реальный URL)
5. Сохраните

### Если разворачиваете Backend (Web Service):

1. Откройте настройки вашего сервиса на Render
2. Перейдите в раздел **Environment**
3. Добавьте каждую переменную по очереди:

   **Переменная 1:**
   - **Key:** `DATABASE_URL`
   - **Value:** Ваш connection string (от Neon/Supabase/Railway)

   **Переменная 2:**
   - **Key:** `PORT`
   - **Value:** `3001`

   **Переменная 3:**
   - **Key:** `JWT_SECRET`
   - **Value:** Сгенерируйте случайную строку (минимум 32 символа)
     - Можно использовать: https://randomkeygen.com/
     - Или команду: `openssl rand -base64 32`

   **Переменная 4:**
   - **Key:** `NODE_ENV`
   - **Value:** `production`

4. Сохраните все переменные

---

## 🔍 Где взять DATABASE_URL?

### Для Neon:
1. Зайдите на https://neon.tech
2. Откройте ваш проект
3. Dashboard → Connection Details
4. Скопируйте Connection String (формат: `postgresql://...`)

### Для Supabase:
1. Зайдите на https://supabase.com
2. Откройте ваш проект
3. Settings → Database
4. Connection string → Connection pooling
5. Скопируйте Connection String

### Для Railway PostgreSQL:
Используйте: `${{Postgres.DATABASE_URL}}`
(Замените `Postgres` на имя вашего PostgreSQL сервиса)

---

## 🔍 Где взять Backend URL?

### Для Railway:
1. Railway Dashboard → ваш backend сервис
2. Settings → Networking
3. Скопируйте Public Domain
4. Добавьте `https://` в начало

**Пример:** `tenderhub-production.up.railway.app` → `https://tenderhub-production.up.railway.app`

### Для Render:
1. Render Dashboard → ваш backend сервис
2. Скопируйте URL из верхней части страницы
3. Обычно выглядит как: `https://your-service.onrender.com`

---

## ✅ Проверка после настройки

### Frontend:
1. Пересоберите проект (Render сделает это автоматически)
2. Откройте консоль браузера (F12)
3. Проверьте, что `NEXT_PUBLIC_API_URL` доступен

### Backend:
1. Проверьте логи в Render Dashboard
2. Должно быть: "Server running on port 3001"
3. Откройте в браузере: `https://your-backend-url.onrender.com/api/health`
4. Должен вернуться: `{"status":"ok","timestamp":"..."}`

---

## 🆘 Частые ошибки

### Ошибка: "JWT_SECRET not configured"
**Решение:** Добавьте переменную `JWT_SECRET` в настройки backend

### Ошибка: "Cannot connect to database"
**Решение:** 
- Проверьте правильность `DATABASE_URL`
- Убедитесь, что БД доступна извне (для Neon/Supabase это включено по умолчанию)

### Ошибка: "API URL не настроен"
**Решение:**
- Убедитесь, что переменная называется точно `NEXT_PUBLIC_API_URL` (с префиксом!)
- Пересоберите frontend после добавления переменной

### Ошибка: CORS
**Решение:** Backend уже настроен для работы с внешними доменами. Если нужно добавить конкретный домен, отредактируйте `backend/src/index.ts`

