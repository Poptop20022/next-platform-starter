# Создание Backend сервиса на Railway - Пошаговая инструкция

## Проблема

У вас нет backend сервиса на Railway, поэтому frontend не может подключиться к API.

**Важно:** Backend URL находится на **Railway**, а не на Netlify!
- **Netlify** = только Frontend (ваш сайт)
- **Railway** = Backend (API сервер)

## Решение: Создайте Backend сервис

### Вариант 1: Из GitHub репозитория (Рекомендуется)

1. **Откройте Railway Dashboard:**
   - https://railway.app
   - Войдите в аккаунт

2. **Создайте новый проект:**
   - Нажмите **"New Project"**
   - Выберите **"Deploy from GitHub repo"**
   - Авторизуйте Railway для доступа к GitHub (если нужно)

3. **Выберите репозиторий:**
   - Найдите ваш репозиторий `next-platform-starter`
   - Нажмите на него

4. **Railway автоматически определит структуру:**
   - Если не определил, укажите вручную:
   - **Root Directory:** `backend`
   - **Build Command:** `npm install && npm run build`
   - **Start Command:** `npm start`

5. **Railway начнет деплой:**
   - Подождете завершения (2-3 минуты)
   - Проверьте логи на наличие ошибок

### Вариант 2: Empty Service + GitHub

1. **Railway Dashboard** → ваш проект (или создайте новый)

2. **+ New** → **Empty Service**

3. **Settings** → **Source:**
   - Выберите **GitHub**
   - Выберите ваш репозиторий
   - **Root Directory:** `backend`

4. **Settings** → **Build:**
   - **Build Command:** `npm install && npm run build`
   - **Start Command:** `npm start`

5. Railway автоматически начнет деплой

## Настройка переменных окружения

После создания сервиса:

1. **Railway Dashboard** → ваш backend сервис

2. **Variables** (вкладка слева)

3. **+ New Variable** - добавьте:

   ```
   DATABASE_URL = postgresql://neondb_owner:npg_YkX7Wm6FNRTU@ep-mute-truth-aejonffm-pooler.c-2.us-east-2.aws.neon.tech/neondb?channel_binding=require&sslmode=require
   ```

   ⚠️ **Важно:** Используйте ваш реальный Neon connection string!

4. Добавьте остальные переменные:

   ```
   PORT = 3001
   JWT_SECRET = ваш-длинный-секретный-ключ-минимум-32-символа
   NODE_ENV = production
   ```

5. Railway автоматически пересоберет сервис

## Получение Public Domain

1. **Railway Dashboard** → ваш backend сервис

2. **Settings** → **Networking**

3. Если нет Public Domain:
   - Нажмите **"Generate Domain"**
   - Railway создаст публичный URL

4. Скопируйте **Public Domain**
   - Например: `tenderhub-production.up.railway.app`
   - Полный URL: `https://tenderhub-production.up.railway.app`

## Проверка работы Backend

1. **Проверьте логи:**
   - **Deployments** → последний deployment → **View Logs**
   - Должно быть: "Server running on port 3001"
   - Должно быть: "Database connection established"

2. **Проверьте доступность:**
   - Откройте в браузере: `https://your-backend.railway.app/api/health`
   - Должен вернуться: `{"status":"ok","timestamp":"..."}`

3. **Если ошибка:**
   - Проверьте переменные окружения
   - Проверьте логи на ошибки
   - Убедитесь, что DATABASE_URL правильный

## Выполнение миграций

1. **Railway Dashboard** → ваш backend сервис

2. **Deployments** → **Shell** (или **View Logs** → **Shell**)

3. Выполните команды:

   ```bash
   npm run migrate
   ```

   Должно быть: "Migration completed successfully"

4. Создайте пользователя:

   ```bash
   npm run create-admin admin@example.com admin123 "Admin User"
   ```

   Должно быть: "Admin user created successfully"

## Настройка Frontend

После получения backend URL:

1. **Netlify Dashboard** → ваш сайт

2. **Site settings** → **Environment variables**

3. Добавьте:
   ```
   NEXT_PUBLIC_API_URL = https://your-backend.railway.app
   ```
   (URL из шага "Получение Public Domain")

4. **Пересоберите сайт:**
   - **Deploys** → **Trigger deploy** → **Clear cache and deploy site**

## Итоговая проверка

1. ✅ Backend доступен: `https://your-backend.railway.app/api/health`
2. ✅ Миграции выполнены
3. ✅ Пользователь создан
4. ✅ NEXT_PUBLIC_API_URL настроен в Netlify
5. ✅ Frontend пересобран
6. ✅ Вход работает на странице входа

## Troubleshooting

### Backend не запускается

**Проверьте:**
- Логи в Railway Dashboard
- Переменные окружения (особенно DATABASE_URL)
- Build Command и Start Command правильные

### Ошибка подключения к БД

**Проверьте:**
- DATABASE_URL правильный (Neon connection string)
- Neon база данных активна
- SSL настройки (`sslmode=require`)

### 404 на /api/health

**Проверьте:**
- Backend запущен (логи показывают "Server running")
- Public Domain правильный
- URL начинается с `https://`

### CORS ошибки

**Проверьте:**
- В `backend/src/index.ts` добавлен ваш Netlify домен в `allowedOrigins`
- Backend пересобран после изменения CORS

## Что дальше?

После настройки:
1. Backend будет доступен по URL типа `https://your-backend.railway.app`
2. Frontend будет подключаться к этому URL
3. Backend будет использовать Neon базу данных
4. Всё будет работать! 🎉

