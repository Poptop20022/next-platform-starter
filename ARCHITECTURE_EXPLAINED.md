# Архитектура TenderHub - Подробное объяснение

## 🏗️ Общая архитектура

Ваше приложение состоит из **3 частей**, которые работают на разных сервисах:

```
┌─────────────────┐
│   Frontend      │  →  Netlify (Next.js)
│   (Next.js)     │     https://tehnogrupp.netlify.app
└────────┬────────┘
         │ HTTP запросы
         │ (API calls)
         ▼
┌─────────────────┐
│   Backend API   │  →  Railway (Node.js/Express)
│   (Express)     │     https://your-backend.railway.app
└────────┬────────┘
         │ SQL запросы
         │ (DATABASE_URL)
         ▼
┌─────────────────┐
│   PostgreSQL    │  →  Neon (База данных)
│   (Database)    │     postgresql://neon...
└─────────────────┘
```

## 📦 Что где находится

### 1. Frontend (Next.js) → Netlify ✅
**Что это:** Интерфейс пользователя (страницы, формы, кнопки)
**Где:** Netlify
**URL:** `https://tehnogrupp.netlify.app`
**Файлы:** `app/`, `components/`, `styles/`

**Зачем Netlify:**
- Бесплатный хостинг для статических сайтов
- Автоматический деплой из GitHub
- CDN для быстрой загрузки

### 2. Backend API (Express) → Railway ✅
**Что это:** Серверное приложение, которое:
- Обрабатывает API запросы (`/api/tenders`, `/api/auth/login`)
- Подключается к базе данных
- Выполняет бизнес-логику
- Возвращает данные в JSON

**Где:** Railway (или Render, Fly.io)
**URL:** `https://your-backend.railway.app` (нужно получить)
**Файлы:** `backend/src/`

**Зачем Railway:**
- Netlify НЕ поддерживает долгоживущие серверные процессы
- Express нужен отдельный сервер
- Railway предоставляет Node.js окружение

### 3. PostgreSQL → Neon ✅
**Что это:** База данных, где хранятся:
- Пользователи
- Тендеры
- Лоты, позиции
- КП и т.д.

**Где:** Neon
**URL:** `postgresql://neondb_owner:...@ep-mute-truth-aejonffm-pooler...`
**Зачем Neon:**
- Бесплатный tier
- Управляемая PostgreSQL
- Автоматические бэкапы

## ❌ Что НЕ нужно

### Railway PostgreSQL - НЕ НУЖЕН!
Вы используете Neon, поэтому Railway PostgreSQL можно удалить.

## 🔑 Важно: Backend URL vs Database URL

**Это РАЗНЫЕ вещи!**

- **Backend URL** - URL вашего API сервера (например: `https://backend.railway.app`)
  - Нужен для frontend (Netlify)
  - Находится в Railway → backend сервис → Settings → Networking → Public Domain
  
- **Database URL** - Connection string к базе данных (например: `postgresql://neon...`)
  - Нужен для backend (Railway)
  - Находится в Neon Dashboard → Connection Details

📖 **Подробнее:** [BACKEND_URL_VS_DATABASE_URL.md](./BACKEND_URL_VS_DATABASE_URL.md)

## 🔄 Как они взаимодействуют

### Пример: Пользователь входит в систему

1. **Frontend (Netlify):**
   ```
   Пользователь вводит email/password
   → Нажимает "Войти"
   → Frontend отправляет POST запрос на Backend URL
   → https://backend.railway.app/api/auth/login
   ```

2. **Backend (Railway):**
   ```
   Получает запрос: POST /api/auth/login
   → Проверяет credentials
   → Использует Database URL для подключения к Neon
   → Делает SQL запрос: SELECT * FROM users WHERE email = ...
   → Возвращает JWT token
   ```

3. **Frontend (Netlify):**
   ```
   Получает token
   → Сохраняет в localStorage
   → Перенаправляет на /tenders
   ```

## 🚀 Что нужно настроить

### Шаг 1: Backend на Railway

**Проблема:** Сейчас у вас, возможно, нет backend сервиса на Railway.

**Решение:**

📖 **Подробная инструкция:** [SETUP_BACKEND_RAILWAY.md](./SETUP_BACKEND_RAILWAY.md)

**Кратко:**

1. **Railway Dashboard** → ваш проект
2. **+ New** → **GitHub Repo** (или **Empty Service**)
3. Если через GitHub:
   - Выберите ваш репозиторий
   - Railway автоматически определит `backend/` папку
   - Или укажите **Root Directory:** `backend`
4. Если Empty Service:
   - **Settings** → **Source** → укажите `backend/` как root
   - Или подключите GitHub репозиторий

5. **Настройте переменные окружения:**
   ```
   DATABASE_URL = ваш Neon connection string (из Neon Dashboard)
   PORT = 3001
   JWT_SECRET = ваш-секретный-ключ
   NODE_ENV = production
   ```

6. **Railway автоматически:**
   - Установит зависимости (`npm install`)
   - Соберет проект (`npm run build`)
   - Запустит сервер (`npm start`)

7. **Получите Backend URL (Public Domain):**
   - **Settings** → **Networking**
   - Нажмите **Generate Domain** (если нет)
   - Скопируйте URL (например: `tenderhub-production.up.railway.app`)
   - **Это и есть Backend URL!** Добавьте `https://` в начало

### Шаг 2: Удалите Railway PostgreSQL (если есть)

1. **Railway Dashboard** → найдите PostgreSQL сервис
2. **Settings** → **Delete Service**
3. Это освободит ресурсы

### Шаг 3: Настройте Frontend

1. **Netlify Dashboard** → ваш сайт
2. **Site settings** → **Environment variables**
3. Добавьте:
   ```
   NEXT_PUBLIC_API_URL = https://your-backend.railway.app
   ```
   ⚠️ **Важно:** Это **Backend URL** из шага 1, пункт 7 (НЕ Database URL!)

4. **Пересоберите сайт**

### Шаг 4: Выполните миграции

1. **Railway Dashboard** → ваш backend сервис
2. **Deployments** → **Shell**
3. Выполните:
   ```bash
   npm run migrate
   npm run create-admin admin@example.com admin123
   ```

## ✅ Итоговая конфигурация

### Railway (Backend):
- ✅ Node.js сервис (backend/)
- ✅ Переменные: 
  - `DATABASE_URL` = ваш Neon connection string (Database URL)
  - `PORT` = 3001
  - `JWT_SECRET` = ваш-секретный-ключ
- ✅ Public Domain: `https://your-backend.railway.app` ← **Это Backend URL!**
- ❌ PostgreSQL сервис (удалить, если есть)

### Neon:
- ✅ PostgreSQL база данных
- ✅ Connection string (Database URL) → используется в Railway как `DATABASE_URL`

### Netlify (Frontend):
- ✅ Next.js приложение
- ✅ `NEXT_PUBLIC_API_URL` = `https://your-backend.railway.app` ← **Это Backend URL!**

## 🔍 Проверка

1. **Backend доступен (используйте Backend URL):**
   ```
   https://your-backend.railway.app/api/health
   → {"status":"ok","timestamp":"..."}
   ```
   ⚠️ Замените `your-backend.railway.app` на ваш реальный Backend URL из Railway!

2. **Frontend подключается:**
   - Откройте страницу входа
   - Введите **Backend URL** (НЕ Database URL!)
   - Нажмите "Проверить подключение"
   - Должно быть: ✅ Backend доступен!

3. **Вход работает:**
   - Email: `admin@example.com`
   - Password: `admin123`

## ❓ Частые вопросы

### Q: Почему нельзя всё на Netlify?
**A:** Netlify поддерживает только статические сайты и serverless функции. Express backend требует долгоживущий процесс, который Netlify не поддерживает.

### Q: Почему нельзя всё на Railway?
**A:** Можно! Но Netlify лучше для frontend (бесплатно, быстрее, CDN). Railway можно использовать для обоих.

### Q: Зачем Neon, если Railway есть PostgreSQL?
**A:** Neon бесплатный и достаточен для начала. Railway PostgreSQL платный. Но можно использовать Railway PostgreSQL, если хотите.

### Q: Могу ли я использовать другую БД?
**A:** Да! Supabase, PlanetScale, AWS RDS - любой PostgreSQL. Просто измените DATABASE_URL.

## 📝 Чек-лист

- [ ] Backend сервис создан на Railway
- [ ] DATABASE_URL настроен (Neon connection string)
- [ ] Backend запущен и доступен (проверьте /api/health)
- [ ] Railway PostgreSQL удален (если был)
- [ ] NEXT_PUBLIC_API_URL настроен в Netlify
- [ ] Frontend пересобран
- [ ] Миграции выполнены
- [ ] Пользователь создан
- [ ] Вход работает

